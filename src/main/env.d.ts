/** Injected by electron-vite `define` (see electron.vite.config.ts). Absent
 *  when src/main code runs unbundled (scripts/stress) — guard with typeof. */
declare const __BUILD_INFO__: { time: string; commit: string }
